# Server
PORT=8080

# Database (MySQL)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=flashsale_db
DB_USER=root
DB_PASS=your_db_password
DB_DIALECT=mysql

# Redis (use localhost or your Redis Cloud URL)
# Example for local Redis:
REDIS_URL=redis://localhost:6379
# Example for Redis Cloud (replace with your real URL):
# REDIS_URL=redis://default:password@your-host:your-port

# Auth / CORS
JWT_SECRET=super_secret_jwt_key_change_me
CORS_ORIGIN=http://localhost:5173

# Admin credentials (used by seeder)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=admin123